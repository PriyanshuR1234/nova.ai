# Core web dependencies - fixed versions for compatibility
flask==2.0.1
gunicorn==20.1.0
werkzeug==2.0.3
# Pin setuptools to avoid pkg_resources deprecation warning
setuptools<81.0.0
gtts==2.2.4
# Removing playsound as it's causing build issues on Render
# playsound==1.3.0
# Use SpeechRecognition instead of speech_recognition
SpeechRecognition==3.8.1
undetected-chromedriver==3.5.5
selenium==4.10.0
webdriver-manager==3.8.6
requests==2.28.1
python-dotenv==0.21.0
# Chrome dependencies for headless operation on Render will be handled by the Render environment
# No need to install chromium-browser as a Python package